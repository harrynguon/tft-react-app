// const initialState = {
//     // Now Grab Data from API
// };

const rootReducer = (state, action) => {
    switch (action.type) {
        default: return state;
    }
}

export default rootReducer;